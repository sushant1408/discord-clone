const AuthLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div className="bg-[#5765f2] flex h-full items-center justify-center">{children}</div>
  );
};

export default AuthLayout;
